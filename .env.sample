# ---------- DIGS APP ENV SAMPLE ----------
# Copy to .env and fill in real values (never commit .env)

# API server
API_PORT=4000
VITE_API_URL=http://localhost:4000/api

# SQL Server connection
DB_SERVER=your-sql-server-hostname
DB_USER=your-db-username
DB_PASSWORD=your-db-password
DB_NAME=your-database-name

# Optional: set to false to disable TLS encryption (not recommended)
DB_ENCRYPT=true

# Data source priority (comma list: connector,api,mock)
VITE_CASES_SOURCE_PRIORITY=connector,api,mock

# (Optional) Azure AD Integrated auth hint variables (if using managed identity or AAD)
# DB_AUTH_MODE=SQL            # or AAD
# DB_AAD_CLIENT_ID=your-app-client-id
# DB_AAD_TENANT_ID=your-tenant-id
